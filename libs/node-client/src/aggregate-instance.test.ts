describe('AggregateInstance', () => {

  describe('#reload', () => {
    test.concurrent.todo('update to the latest state')
    test.concurrent.todo('no events')
    test.concurrent.todo('multiple concurrent execution')
  });

  describe('#process', () => {
    test.concurrent.todo('process a command')
    test.concurrent.todo('invalid command')
    test.concurrent.todo('multiple concurrent execution')
  });
});
